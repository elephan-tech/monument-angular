import { Media } from './media';
export type Events = {
  date: string,
  display: boolean,
  eventLink: string,
  file: Media,
  id: string,
  localAsset: string,
  name: string,
  __typename: string,
}
